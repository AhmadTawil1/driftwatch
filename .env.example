# Postgres
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_HOST=
POSTGRES_PORT=

# Provider credentials (names referenced by rows in the `providers` table —
# never store the key values themselves in the database)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Alerting
SLACK_WEBHOOK_URL=

# Cost controls
NIGHTLY_COST_CAP_USD=

# Airflow
AIRFLOW__CORE__FERNET_KEY=
AIRFLOW__WEBSERVER__SECRET_KEY=
